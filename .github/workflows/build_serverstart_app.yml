name: Build VCamServerStart App

on:
  push:
    branches: [ master, main ]
    paths:
      - 'serverstart-app/**'
      - '.github/workflows/build_serverstart_app.yml'
  pull_request:
    branches: [ master, main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Roothide Theos
        run: |
          git clone --recursive https://github.com/roothide/theos.git "$HOME/theos"
          echo "THEOS=$HOME/theos" >> "$GITHUB_ENV"

      - name: Install packaging dependencies
        run: brew install ldid dpkg xz

      - name: Build app
        working-directory: serverstart-app
        run: make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=roothide

      - name: Inspect package
        working-directory: serverstart-app
        run: |
          for package in packages/*.deb; do
            dpkg-deb -I "$package"
            dpkg-deb -c "$package"
          done

      - uses: actions/upload-artifact@v4
        with:
          name: vcamserverstart-app-roothide-deb
          path: serverstart-app/packages/*.deb
          if-no-files-found: error
